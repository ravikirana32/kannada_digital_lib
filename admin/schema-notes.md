# Admin Schema Notes

The editor must use:
`data/sarvajna/metadata/tripadi.schema.json`

It should validate before saving and should never allow `published` status without source verification and rights review.
