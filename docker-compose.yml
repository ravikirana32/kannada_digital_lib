services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sarvajna
    ports: ["5432:5432"]
    volumes: [sarvajna_pg:/var/lib/postgresql/data]
volumes:
  sarvajna_pg:
